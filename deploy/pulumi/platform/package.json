{
  "name": "recollect-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Pulumi IaC for Recollect's PLATFORM stack — the per-release launch host (EC2 + Cloudflare Tunnel; pulls the server image from FOUNDATION's ECR).",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@pulumi/aws": "^7.34.0",
    "@pulumi/cloudflare": "^6.17.0",
    "@pulumi/pulumi": "^3.247.0",
    "@pulumi/random": "^4.21.0"
  }
}
