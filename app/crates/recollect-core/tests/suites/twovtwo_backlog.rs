//! 2v2 rulings that ride per-slot projection or the 6×6 Dusk, held as contracts.

#[test]
#[ignore = "four-seat views and transport contract — PlayerView per slot, the server's 4-socket 2v2 lobby, client 6×6 render, on top of the ready engine surface"]
fn four_seat_views_and_transport() {}
