//! Red contracts for effect tranches the executor does not yet run, held as
//! specs: auras → replacements-before-journaling → meta.

#[test]
#[ignore = "meta-effect contract: the RuleException dispatch is live (the exception_active chokepoint); EvolveIgnoresSharedImprint/AllImprintsShared + PartingTriggersTwice resolve (rule_exceptions.rs); GlimpseLooksOneMore + BondsCostZero are routed (evolution_arrivals.rs/carrier_exceptions.rs). The carriers still routed through a base mechanic are RitualsExtraTarget, MobileMovesTwoFromHere, PairSharesImprints, BondHoldsUnlessSeparated, PartingReclaimsFullCost, and EvolveArrivesBuffed (e.g. PartingReclaimsFullCost rides reclaim-on-fade; the Bond carriers ride PairAdjacent/BondedPair)."]
fn meta_effects_resolve_last() {}
