See @AGENTS.md — the canonical, tool-agnostic guide for working on this
repository. Everything there applies to Claude Code sessions: the documents
are law, the invariants are tested, and `make test && make catalog-check`
gates every change.
